name: Build executables

# PyInstaller can't cross-compile, so each binary is built on its own native
# runner. Produces macOS, Linux and Windows executables as workflow artifacts,
# and attaches them to a GitHub Release when you push a v* tag.

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

permissions:
  contents: write  # needed to attach binaries to a Release on tag builds

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            bin: dist/TRC_Tally_Cloud_Client
            out: TRC_Tally_Cloud_Client-macos
          - os: ubuntu-latest
            bin: dist/TRC_Tally_Cloud_Client
            out: TRC_Tally_Cloud_Client-linux
          - os: windows-latest
            bin: dist/TRC_Tally_Cloud_Client.exe
            out: TRC_Tally_Cloud_Client-windows.exe
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pyinstaller

      - name: Build
        run: pyinstaller --clean -y TRC_Tally_Cloud_Client.spec

      - name: Rename binary per OS
        shell: bash
        run: mv "${{ matrix.bin }}" "${{ matrix.out }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.out }}
          path: ${{ matrix.out }}
          if-no-files-found: error

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.out }}
